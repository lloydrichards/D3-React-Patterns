import React, { ReactElement } from 'react'

interface Props {
    
}

const XTimeAxis = ({}: Props): ReactElement =>{
    return (
        <div>
            
        </div>
    )
}

export default XTimeAxis

